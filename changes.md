Implement the following changes in the repo:

- Rename gpt.py to gemini.py. Inside the file, rename the function call_gpt to call_gemini. In cmdhelper.py, update the import to from gemini import call_gemini and update all call sites. In the hmm bash script and install.sh, replace all references to gpt.py with gemini.py.
- In gemini.py (formerly gpt.py), rename CURRENT_SCIPT_DIR to CURRENT_SCRIPT_DIR in the Config class and in all places it is referenced.
- In gemini.py, in the except block of call_gemini, add return f"#Error: {e}" so the function always returns a string.
- In cmdhelper.py, remove the line single_line_result = str(result).replace('\n', ' ').replace('\r', '').strip() and replace it with single_line_result = str(result).strip().
- In the hmm bash script, add a check after capturing GENERATED_CMD in the -x block: if the output starts with #, print the message in red using ANSI escape codes, then print a second line stating To run this command manually, copy and paste it into your terminal., then exit 0 without calling eval. Place this check immediately after the line GENERATED_CMD=$("$HELPER_SCRIPT" "$@") and before any eval or confirmation logic.
- In the hmm bash script, remove the commented-out safety confirmation block and the standalone eval "$GENERATED_CMD" line. Replace them with a direct eval "$GENERATED_CMD" that only executes if the #-prefix check above did not trigger. No confirmation prompt is needed for normal commands.
- In install.sh, remove all Conda-related logic: the CONDA_BASE detection loop, the conda env list / conda create / conda run blocks, and the CONDA_PYTHON variable assignment. Replace them with the following: detect python3 on the system PATH; create a virtual environment at ~/.local/share/hmm/venv using python3 -m venv; activate it; run pip install -r requirements.txt inside it; set VENV_PYTHON="$HOME/.local/share/hmm/venv/bin/python" for use in the shebang-patching step that follows.
- In install.sh, update the shebang-patching section to use $VENV_PYTHON instead of $CONDA_PYTHON when rewriting the #! lines in gemini.py and cmdhelper.py.
- In install.sh, remove the block that writes export GOOGLE_API_KEY=... into the user's shell config file (~/.bashrc, ~/.zshrc, or ~/.profile). Replace it with: create the directory ~/.config/hmm/ if it does not exist; write the API key into ~/.config/hmm/.env in the format GOOGLE_API_KEY="<key>". If the file already exists, update the GOOGLE_API_KEY line in place.
- In install.sh, keep the double-quoted form export GOOGLE_API_KEY="$GOOGLE_API_KEY" only for the inline export that applies the key to the current install session. Do not persist it in any shell config file.
- In gemini.py, at the top of the call_gemini function (before reading the API key), add a fallback config load: if os.getenv("GOOGLE_API_KEY") is empty or None, attempt to read ~/.config/hmm/.env, parse the GOOGLE_API_KEY="..." line, and set it into os.environ["GOOGLE_API_KEY"]. If neither source provides a key, return "#Error: No API key found. Set GOOGLE_API_KEY or add it to ~/.config/hmm/.env".
- In the hmm bash script's -update block, replace all references to CONDA_PYTHON and the conda-env shebang detection logic with equivalent logic that reads the venv Python path from ~/.local/share/hmm/venv/bin/python and uses that for shebang restoration.
- In the README, fix "the right commans is returned" to "the right command is returned" in the repo description / About section.
- In the README, fix "followd" to "followed".
- In the README, update the "What the Installation Does" section to reflect the new installation approach: replace all references to Conda/Miniconda with the venv-based setup, mention ~/.local/share/hmm/venv as the environment location, and replace the mention of shell config API key injection with the new ~/.config/hmm/.env storage.
- Add a requirements.txt file to the repo root containing a single line: google-genai.
